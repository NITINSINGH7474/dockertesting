# Scholarship Management Project with Docker

## Steps to Run:
1. Make sure Docker Desktop is running.
2. Build the image:
   `docker build -t my-cpp-app .`
3. Run the container:
   `docker run my-cpp-app`